services:
  devcontainer:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../../snap:/workspaces/snap:cached
      - /var/run/docker.sock:/var/run/docker.sock
    privileged: true
    command: sleep infinity
