Best practice is to use the VS Code Dev-Container feature.

Inside the running container:

`cd ${ROOT}/pokerth`
`bash docker/windows/build_windows.sh`

The end result is a setup for windows - see build_windows.sh


